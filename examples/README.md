# Example document

`scanned_ocr_demo.pdf` is a synthetic, image-only PDF created for testing the local OCR fallback. It contains no private or third-party content.

After installing OCR, index it and ask:

```text
Başvurunun sonucu nedir?
```
